---
layout: layouts/base.njk
title: About / Resume
description: A living, human-readable resume with experience, skills, and highlights.
---

<h1 class="text-3xl font-semibold mb-4">About</h1>

<div class="prose prose-invert max-w-none leading-relaxed text-white">
  {{ site.bio | markdownify | safe }}
</div>

<h2 class="text-2xl font-semibold mt-10 mb-4">Experience</h2>
<ul class="space-y-6">
  {% for role in resume.experience %}
  <li class="p-5 rounded-2xl border border-slate-800">
    <div class="flex flex-wrap items-baseline gap-2">
      <h3 class="font-semibold">{{ role.title }}</h3>
      <span class="text-slate-400">· {{ role.company }}</span>
      <span class="text-sm text-slate-400">· {{ role.period }}</span>
    </div>
    {% if role.summary %}<p class="mt-2 text-slate-300">{{ role.summary }}</p>{% endif %}
    {% if role.impact %}
      <ul class="mt-3 list-disc pl-5 text-sm space-y-1">
        {% for point in role.impact %}<li>{{ point }}</li>{% endfor %}
      </ul>
    {% endif %}
    {% if role.tags %}
      <div class="mt-3 flex flex-wrap gap-2">
        {% for t in role.tags %}<span class="px-2 py-1 rounded-full text-xs border border-slate-700">{{ t }}</span>{% endfor %}
      </div>
    {% endif %}
  </li>
  {% endfor %}
</ul>

<h2 class="text-2xl font-semibold mt-10 mb-4">Skills</h2>
<div class="flex flex-wrap gap-2">
  {% for s in resume.skills %}
    <span class="px-3 py-1 rounded-full border text-sm border-slate-700">{{ s }}</span>
  {% endfor %}
</div>

<h2 class="text-2xl font-semibold mt-10 mb-4">Education</h2>
<ul class="space-y-3">
{% for ed in resume.education %}
  <li>
    <div class="font-medium">{{ ed.degree }}</div>
    <div class="text-sm text-slate-400">{{ ed.institution }} · {{ ed.period }}</div>
  </li>
{% endfor %}
</ul>

<h2 class="text-2xl font-semibold mt-10 mb-4">Selected Writing</h2>
<p>Find all posts on <a class="hover:underline" href="{{ site.social.medium }}" target="_blank" rel="noopener">Medium</a>. Highlights:</p>
<ul class="mt-3 list-disc pl-6 space-y-1">
  {% for w in resume.writing %}
  <li><a class="hover:underline" href="{{ w.url }}" target="_blank" rel="noopener">{{ w.title }}</a> <span class="text-sm text-slate-400">· {{ w.note }}</span></li>
  {% endfor %}
</ul>

<h2 class="text-2xl font-semibold mt-10 mb-4">Publications</h2>
<p>See the full list on the <a class="hover:underline" href="/publications/">Publications</a> page.</p>
<ul class="mt-3 list-disc pl-6 space-y-1">
  {% for p in resume.publication_highlights %}
  <li><a class="hover:underline" href="{{ p.url }}" target="_blank" rel="noopener">{{ p.title }}</a> <span class="text-sm text-slate-400">· {{ p.venue }}</span></li>
  {% endfor %}
</ul>